#include "main.h"

/**
 * mul - checks for a digit (0 through 9)
 * @a: first operand
 * @b: second operand
 *
 * Return: Returns 1 if true and 0 if false
 *
 */

int mul(int a, int b)
{
	return (a * b);
}
